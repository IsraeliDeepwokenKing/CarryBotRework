services:
  - type: web
    name: deepwoken-carry-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py

    envVars:
      - key: TOKEN
        sync: false